% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ctof.R
\name{ctof}
\alias{ctof}
\title{Convert Celsius to Fahrenheit (lightweight unit attributes)}
\usage{
ctof(c, ignoreattr = FALSE, quiet = TRUE, return_original_as_attr = TRUE, ...)
}
\arguments{
\item{c}{Numeric vector of temperatures in Celsius. May carry \code{attr(., "unit")}.}

\item{ignoreattr}{Logical; if \code{FALSE}, require and validate \code{attr(c, "unit")} is Celsius.}

\item{quiet}{Logical; if \code{FALSE}, emit a short message about the conversion.}

\item{return_original_as_attr}{Logical; if \code{TRUE}, attach \code{attr(out, "original")}
containing the original numeric input (assumed/validated Celsius).}

\item{...}{Reserved for backward compatibility (ignored).}
}
\value{
Numeric vector of temperatures in Fahrenheit. The result carries
\code{attr(out, "unit") = "degF"}. If \code{return_original_as_attr = TRUE},
\code{attr(out, "original")} holds the original numeric Celsius values.
}
\description{
Converts temperatures expressed in Celsius to Fahrenheit using lightweight unit
handling via \code{attr(x, "unit")}. This function does not use the \pkg{units}
package.
}
\section{Unit handling}{

\itemize{
\item If \code{ignoreattr = FALSE} (default), \code{c} must have
\code{attr(c, "unit")} that resolves to \code{"degC"}; otherwise an error is thrown.
\item If \code{ignoreattr = TRUE}, the attribute is ignored and the input is
assumed to be Celsius.
\item Output is a numeric vector in Fahrenheit with \code{attr(out, "unit") = "degF"}.
}
}

\examples{
x <- c(0, 25, 30)
attr(x, "unit") <- "degC"
y <- ctof(x)
y
attr(y, "unit")  # "degF"
attr(y, "original")

# Ignore missing attribute (assume Celsius)
ctof(c(0, 25), ignoreattr = TRUE)

}
