% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/avgwdir.R
\name{avgwdir}
\alias{avgwdir}
\title{Moving-window average wind direction}
\usage{
avgwdir(winddirectionsDeg, windspeeds, movingWindow = 10, na.pad = TRUE)
}
\arguments{
\item{winddirectionsDeg}{Numeric vector. Wind direction in degrees (0–360).}

\item{windspeeds}{Numeric vector. Wind speed (same length as
\code{winddirectionsDeg}).}

\item{movingWindow}{Integer. Window size (number of samples) for the rolling sum.}

\item{na.pad}{Logical. If \code{TRUE}, pads the leading window with \code{NA} values to
preserve input length; if \code{FALSE}, returns only the fully-defined values.}
}
\value{
Numeric vector of averaged wind direction degrees in [0, 360).
}
\description{
Computes a rolling-mean wind direction using vector (u/v-style) averaging:
directions are converted to north/south and east/west components, summed in a
rolling window with weights given by wind speed, and converted back to a
direction in degrees.
}
\details{
This function performs a rolling sum on weighted wind components and converts
back to a direction via \code{atan2()}. When \code{na.pad = TRUE}, the result length
equals the input length.
}
\examples{
wd  <- c(350, 10, 15, 20, 25)
wsp <- c(5,   5,  5,  5,  5)
avgwdir(wd, wsp, movingWindow = 3)

}
