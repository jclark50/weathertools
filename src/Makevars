#PKG_CPPFLAGS += $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript" -e "RcppParallel::CxxFlags()")
#PKG_LIBS     += $(shell "${R_HOME}/bin${R_ARCH_BIN}/Rscript" -e "RcppParallel::RcppParallelLibs()")
PKG_CPPFLAGS += $(shell "$(R_HOME)/bin/Rscript" -e "RcppParallel::CxxFlags()")

PKG_LIBS += $(shell "$(R_HOME)/bin/Rscript" -e "cat(' -L', shQuote(system.file('libs', package='RcppParallel')), ' -lRcppParallel', ' ', RcppParallel::RcppParallelLibs(), sep='')")